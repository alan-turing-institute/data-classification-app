import pytest


# @pytest.fixture(scope="session")
# def django_db_use_migrations(django_db_use_migrations):
#     """For the migrations test folder override the --nomigrations flag so that migrations do run"""
#     return True
