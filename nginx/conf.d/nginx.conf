# first we declare our upstream server, which is our Gunicorn application
upstream haven {
    # docker will automatically resolve this to the correct address
    # because we use the same name as the service: "web"
    server web:8000;
}

upstream authelia {
    server authelia:9091;
}

# underscores_in_headers on;

server {
    server_name auth.app.localhost;
    listen 80;
    return 301 https://$server_name$request_uri;
}

server {
    server_name auth.app.localhost;
    listen 443 ssl http2;
    include /etc/nginx/ssl.conf;

    location / {
        proxy_pass http://authelia;
        include /etc/nginx/proxy.conf;
    }
}


server {
    server_name haven.app.localhost;
    listen 80;
    return 301 https://$server_name$request_uri;
}

server {
    server_name haven.app.localhost;
    listen 443 ssl http2;

    include /etc/nginx/ssl.conf;
    include /etc/nginx/authelia.conf; # Virtual endpoint to forward auth requests

    location /admin {
        proxy_pass http://haven/admin;
	include /etc/nginx/proxy.conf;
    }

    location /static {
        alias /app/staticfiles/;
    }

    location / {
        proxy_pass http://haven;
        include /etc/nginx/auth.conf;
        include /etc/nginx/proxy.conf;
    }
}
